#include <tunables/global>

profile esphome_displayeditor flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  signal (send) set=(kill,term,int,hup,cont),

  network inet stream,
  network inet6 stream,

  # S6 Overlay and Bashio are provided by the Home Assistant base image.
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /usr/lib/bashio/** ix,

  /opt/esphome-displayeditor/bin/python3 ix,
  /opt/esphome-displayeditor/** rix,
  /app/** r,
  /run.sh rix,

  /data/ rw,
  /data/** rwk,
  /homeassistant/esphome/ r,
  /homeassistant/esphome/** rwk,

  /etc/** r,
  /proc/** r,
  /sys/devices/system/cpu/** r,
  /tmp/ rw,
  /tmp/** rwk,
}
